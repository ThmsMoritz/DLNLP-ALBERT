# Extension: Low-Data Robustness

This folder contains the extension experiment. It compares BERT-base and ALBERT-base on different fractions of the training data.

Default task: SST-2.

Run:

```bash
python experiments/low_data_extension/run_low_data_extension.py
```

Results are written to:

```text
results/low_data_results.csv
```
